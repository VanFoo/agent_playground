# devisolation firewall whitelist
#
# One domain per line. Exact hostname match unless prefixed with "." (which
# also matches all subdomains). Prefer the narrowest entry that works —
# avoid broad wildcards like ".amazonaws.com" or ".cloudfront.net" unless
# you genuinely need one; they quietly widen what's allowed for everything
# else too.
#
# After editing this file, reload Squid without restarting the container:
#   docker compose exec firewall squid -k reconfigure
#
# To find out what's missing when something breaks:
#   docker compose logs -f firewall

# Claude Code / Anthropic
api.anthropic.com
console.anthropic.com
claude.ai
statsig.anthropic.com

# npm / Node (Claude Code installs via npm)
registry.npmjs.org

# GitHub / devcontainer features (ghcr.io hosts the Claude Code and
# GitHub CLI features themselves)
github.com
api.github.com
raw.githubusercontent.com
objects.githubusercontent.com
codeload.github.com
ghcr.io
pkg-containers.githubusercontent.com

# OS packages (apt, Ubuntu base image)
archive.ubuntu.com
security.ubuntu.com
ports.ubuntu.com
